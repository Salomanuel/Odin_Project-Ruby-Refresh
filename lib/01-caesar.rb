class CaesarCypher
	def caesar(string, shift=5)
		alphabet = ("a".."z").to_a
		string.each do |l|
			alphabet.index()
		end

		return string
	end
end

# puts caesar("culo")

# put checks for weird chars
# check caps or not